'use strict';

/**
 * user-workshop-detail service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-workshop-detail.user-workshop-detail');
