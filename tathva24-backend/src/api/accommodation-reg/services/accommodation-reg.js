'use strict';

/**
 * accommodation-reg service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::accommodation-reg.accommodation-reg');
